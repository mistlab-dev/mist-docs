server:
  host: "0.0.0.0"
  port: 8900

database:
  host: "127.0.0.1"
  port: 3306
  user: "mist_team"
  password: "MistTeam@2026"
  dbname: "mist_team"
  max_open_conns: 20
  max_idle_conns: 10

jwt:
  secret: "mist-docs-jwt-secret-change-in-production"
  issuer: "mist-docs"
  expire_hours: 24

storage:
  root: "/var/lib/mist-docs/files"
  max_file_size: 52428800    # 50MB
  version_keep: 20
  audit_retain_days: 180

websocket:
  max_message_size: 1048576  # 1MB
  ping_interval: 30          # seconds

log:
  level: "info"
